/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export', // <-- LINE REMOVED
    // ... other configurations (if any)
    // Example: Maybe you need reactStrictMode
    reactStrictMode: true,
  };
  
  export default nextConfig; // or module.exports = nextConfig;